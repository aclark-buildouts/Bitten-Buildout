import os
for file in os.listdir("${source-download:location}")
    filepath = file
    arglist =['-qq', file]
    pep8.process_options(arglist)
    pep8.input_file(file)
    error_stats = pep8.get_error_statistics()
    warning_stats = pep8.get_warning_statistics() 



