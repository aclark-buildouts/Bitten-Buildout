
import os

from settings import (ADMINS, MANAGERS, DATABASE_ENGINE, DATABASE_NAME, DATABASE_USER,
    DATABASE_PASSWORD, DATABASE_HOST, DATABASE_PORT, TIME_ZONE, LANGUAGE_CODE,
    MEDIA_ROOT, MEDIA_URL, ADMIN_MEDIA_PREFIX, SECRET_KEY, MIDDLEWARE_CLASSES,
    ROOT_URLCONF, INSTALLED_APPS, TEMPLATE_LOADERS, TEMPLATE_DIRS,)

INSTALLED_APPS = INSTALLED_APPS + (
    ${local_settings:apps}
    )
